# v2026.7.23.110 更新说明

发布日期：2026-07-23

## 本次修复

- 修复腾讯云 SCF 将百度返回的无时区过期时间误按 UTC 解析的问题，避免有效期被错误延后 8 小时并持续返回已过期 Token。
- 兼容 SCF Python 3.6 运行环境，移除对 `datetime.fromisoformat()` 的依赖。
- COS Token 存储改为每个授权配置独立对象，避免多个项目并发刷新时相互覆盖；保留旧单对象存储的只读兼容。
- 加固并发刷新恢复、应用 ID 一致性、云端记录完整性与返回数据校验，异常记录不再被当作有效 Token 使用。
- 在线更新源和“关于”信息同步为新 GitHub 用户名 `kaiteJiang`。

## 验证结果

- 云端 SCF 已部署后执行百度 API 就绪检查：9 个项目、11 个授权全部通过。
- 11 个授权均从 COS 云端存储读取，刷新链路及新对象前缀已确认可用。
- SCF 部署包审计通过，不包含配置、密钥、日志、报告或其他运行数据。
- 完整自动化测试通过。

## 发布资产

- 完整安装器：`Hourlyreport_automation_setup_v2026.7.23.110.exe`
- 在线更新包：`Hourlyreport_automation_v2026.7.23.110.zip`

在线更新不会覆盖用户的 `configs/`、`secrets/`、`logs/`、`reports/`、`backups/`、`diagnostics/`、`kst_exports/` 或 `browser_profile/`。
